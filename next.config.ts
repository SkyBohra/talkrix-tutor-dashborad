import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // AI Teacher Admin Panel runs on port 3003
  // Subdomain: teacher.talkrix.com or localhost:3003
};

export default nextConfig;
